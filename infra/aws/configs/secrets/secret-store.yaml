
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: secretstore-us-east-1
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: aws-secrets-sa
